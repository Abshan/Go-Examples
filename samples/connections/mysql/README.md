# MySQL Read Sample

This sample connects to MySQL and reads records from `test_table`.

Connection details used:
- Host: `127.0.0.1:3306`
- User: `root`
- Password: `root`
- Database: `test_table`

Run:
```bash
cd samples/connections/mysql
go run .
```
